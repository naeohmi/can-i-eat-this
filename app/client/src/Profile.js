import React, { Component } from 'react';

class Profile extends Component {
  render() {
    return (
      <div className="profile">This is the user profile.</div>
    );
  }
}

export default Profile;